#include <stdio.h>
#define LENGTH 20

int main()
{
    typedef struct node
    {
     char* word;
     struct node* next;
    }
    node;
    node* hashtable[500];
    
    for (int i = 0; i < 500; i++)
    {
        
    }
}
